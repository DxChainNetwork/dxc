# dxc3-genesis-contract

System contracts for DxChain3.0.

## Prepare

install dependency:

```shell
npm i
```

## Compile contracts

```shell
npm run compile
```

Release solidity files will be generated in `artifacts` folder.
